DIR="$(cd "$(dirname ${BASH_SOURCE[0]})" ; pwd -P )"
NAME="$(basename ${BASH_SOURCE[0]})"

CXXFLAGS="'--coverage -DDEBUG -g3 -ggdb -std=c++0x -O0' -ftemplate-backtrace-limit=0" 

source ${DIR}/gcc_base

