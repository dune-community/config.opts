DIR="$(cd "$(dirname ${BASH_SOURCE[0]})" ; pwd -P )"
NAME="$(basename ${BASH_SOURCE[0]})"

CXXFLAGS="'-DDXT_DISABLE_LARGE_TESTS=1 -DDEBUG -g3 -ggdb -std=c++14 -O0 -ftemplate-backtrace-limit=0'"
CXX=zapcc
CC=zapcc
source ${DIR}/clang_base
