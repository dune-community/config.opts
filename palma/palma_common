
CMAKE_FLAGS="-Wno-dev -DUMFPACK_ROOT=${HOME}/local -DCMAKE_BUILD_TYPE='RELEASE' -DSUITESPARSE_INCLUDE_DIR_HINTS=${HOME}/local/include/ -DSUITESPARSE_LIBRARY_DIR_HINTS=${HOME}/local/lib/"
BDIR="$(cd "$(dirname ${BASH_SOURCE[0]})" ; cd ../../ ; pwd -P )"
MAKE_FLAGS="-- -j 12"
BUILDDIR=/scratch_gss/tmp/${USER}/$(basename ${BDIR})/${NAME}
